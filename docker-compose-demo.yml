services:
  demo-data:
    image: mekomsolutions/ozone-demo-data
    environment:
      - SPRING_PROFILES_ACTIVE=prod
      - OPENMRS_SERVERURL=${SERVER_SCHEME}://${O3_HOSTNAME}/openmrs
      - OPENMRS_USERNAME=${OPENMRS_USER}
      - OPENMRS_PASSWORD=${OPENMRS_PASSWORD}
      - OPENMRS_OAUTH_ENABLED=${ENABLE_SSO}
      - OPENMRS_OAUTH_CLIENTID=${OAUTH_CLIENT_ID}
      - OPENMRS_OAUTH_CLIENTSECRET=${OAUTH_CLIENT_SECRET}
      - KEYCLOAK_SERVERURL=${SERVER_SCHEME}://${KEYCLOAK_HOSTNAME}
      - KEYCLOAK_DEMO_DATA_REALM=ozone
      - KEYCLOAK_USERS_JSON_PATH=/app/keycloak/users.json
      - KEYCLOAK_DEMO_DATA_CLIENTID=keycloak-admin-sa
      - KEYCLOAK_DEMO_DATA_CLIENTSECRET=${KEYCLOAK_ADMIN_SA_CLIENT_SECRET}
    restart: on-failure
    networks:
      ozone:
    volumes:
      - ${KEYCLOAK_CONFIG_PATH}/demo:/app/keycloak

networks:
  ozone:
  web:
    external: true
